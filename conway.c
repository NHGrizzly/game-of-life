#include "conway.h"
#include "terminal.h"

int main(int argc, char const *argv[])
{
    terminal_clear();
    terminal_print_at(0,0);
    terminal_print_at(1,15);
    terminal_print_at(2,3);
    return 0;
}

